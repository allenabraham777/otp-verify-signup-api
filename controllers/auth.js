const models = require('../models');

const signup = async (req, res) => {
  res.json({success: true})
}

const login = async (req, res) => {
  res.json({success: true})
}

module.exports = {
  signup,
  login
}